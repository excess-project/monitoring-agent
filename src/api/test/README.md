# README

After building the executable by calling the Makefile, please execute the
<pre>setenv.sh</pre> script in order to set the appropriate environment
variables before continuing with running the generated executable.